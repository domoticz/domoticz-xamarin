<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:resources="clr-namespace:NL.HNOGames.Domoticz.Resources;assembly=NL.HNOGames.Domoticz"
             xmlns:custcontrol="clr-namespace:NL.HNOGames.Domoticz.Controls;assembly=NL.HNOGames.Domoticz"
             xmlns:selector="clr-namespace:NL.HNOGames.Domoticz.ViewSelector;assembly=NL.HNOGames.Domoticz"
             xmlns:range="clr-namespace:Xamarin.RangeSlider.Forms;assembly=Xamarin.RangeSlider.Forms"
             x:Class="NL.HNOGames.Domoticz.Views.Settings.QRCodeSettingsPage"
             Style="{StaticResource ContentPageType}"
             Title="{resources:Translate category_QRCode}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="{resources:Translate add}" Order="Primary" Activated="ToolbarItem_Activated"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Orientation="Vertical" HorizontalOptions="FillAndExpand" VerticalOptions="StartAndExpand">
                <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="StartAndExpand" Orientation="Horizontal" Margin="20">
                    <StackLayout HorizontalOptions="StartAndExpand" VerticalOptions="Center" Orientation="Vertical" >
                        <Label Text="{resources:Translate qrcode_enable_settings}" VerticalOptions="CenterAndExpand" HorizontalOptions="Start"  Style="{StaticResource SettingsDetailType}" />
                    </StackLayout>
                    <Switch x:Name="swEnableQRCode"  VerticalOptions="Center" HorizontalOptions="End"/>
                </StackLayout>

                <ListView x:Name="listView" SeparatorVisibility="None"  ItemSelected="OnItemSelected" RowHeight="130" HasUnevenRows="True">
                    <ListView.ItemTemplate VerticalOptions="StartAndExpand">
                        <DataTemplate>
                            <ViewCell>
                                <Grid Style="{StaticResource DashboardDeviceCardType}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="50"></ColumnDefinition>
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackLayout Grid.Column="0" HorizontalOptions="FillAndExpand" VerticalOptions="Center">
                                        <Switch IsToggled="{Binding Enabled}" Toggled="Switch_Toggled"/>
                                    </StackLayout>
                                    <StackLayout Grid.Column="1" Orientation="Vertical" HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand">
                                        <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand">
                                            <StackLayout Orientation="Vertical" HorizontalOptions="FillAndExpand"  VerticalOptions="CenterAndExpand" Padding="0,10,0,0">
                                                <Label Text="{Binding Name}" LineBreakMode="TailTruncation" Style="{StaticResource HeaderType}" />
                                                <custcontrol:MultiLineLabel Text="{Binding SwitchDescription}" Lines="2" Style="{StaticResource DetailType}" LineBreakMode="WordWrap" />
                                            </StackLayout>
                                            <StackLayout Orientation="Vertical" HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand" Margin="0,-5,5,0">
                                                <StackLayout Orientation="Horizontal" HorizontalOptions="End" VerticalOptions="End">
                                                    <Button x:Name="btnDeleteButton" Text="{resources:Translate delete}" HorizontalOptions="End" VerticalOptions="End" Clicked="btnDeleteButton_Clicked">
                                                        <Button.WidthRequest>
                                                            <OnPlatform x:TypeArguments="x:Double" iOS="100" Android="120" WinPhone="100" />
                                                        </Button.WidthRequest>
                                                        <Button.HeightRequest>
                                                            <OnPlatform x:TypeArguments="x:Double" iOS="35" Android="50" WinPhone="50" />
                                                        </Button.HeightRequest>
                                                    </Button>
                                                </StackLayout>
                                            </StackLayout>
                                        </StackLayout>
                                    </StackLayout>
                                </Grid>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>